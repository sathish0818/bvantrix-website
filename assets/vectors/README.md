# Vectors

| File | Used by |
|---|---|
| `logo.svg`    | Standalone mark. Inherits `currentColor` |
| `favicon.svg` | Browser tab |

The hero orb, the decision web (section 01) and the ESG circles (section 02)
are **inline SVG in `index.html`**, not files — so they inherit the page's
colours and stay crisp at any size.

Sending your own illustrations? Drop them here and tell me which section
they belong to. Outlined strokes and no embedded rasters, please.
